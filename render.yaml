services:
  - type: web
    name: Civle
    env: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: civle-storage
      mountPath: /storage
      sizeGB: 1

